package com.example.weatherapp.adapters.viewholders

import android.view.View

class TodayViewHolder(view: View) : BaseViewHolder(view) {

    override val containerView: View?
        get() = view

    override fun <T> bind(item: T) {

    }
}